# Базовый стек — ставится чисто на Windows/Linux/macOS без браузеров и компиляции тяжёлых пакетов.
fastapi==0.115.0
uvicorn[standard]==0.32.0
sqlalchemy==2.0.35
pydantic>=2.10,<3
pydantic-settings>=2.7,<3
httpx==0.27.2
curl_cffi==0.7.4
selectolax==0.3.25
apscheduler==3.10.4
python-telegram-bot==21.6
python-dotenv==1.0.1

# Опциональный браузерный фолбэк (для сайтов с тяжёлым антиботом) — НЕ обязателен:
#   pip install -r requirements-browser.txt
#   playwright install chromium
